// Code generated by fhir-gen; DO NOT EDIT.

package r4

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// DeviceUseStatementResourceType is the FHIR resourceType discriminator for DeviceUseStatement.
const DeviceUseStatementResourceType = "DeviceUseStatement"

// DeviceUseStatement is the generated FHIR DeviceUseStatement resource.
type DeviceUseStatement struct {
	DomainResource
	Identifier        []Identifier              `json:"identifier,omitempty"`
	BasedOn           []Reference               `json:"basedOn,omitempty"`
	Status            *DeviceUseStatementStatus `json:"status,omitempty"`
	StatusElement     *fhir.PrimitiveElement    `json:"-"`
	Subject           *Reference                `json:"subject,omitempty"`
	DerivedFrom       []Reference               `json:"derivedFrom,omitempty"`
	TimingTiming      *Timing                   `json:"timingTiming,omitempty"`
	TimingPeriod      *Period                   `json:"timingPeriod,omitempty"`
	TimingDateTime    *FHIRDateTime             `json:"timingDateTime,omitempty"`
	RecordedOn        *string                   `json:"recordedOn,omitempty"`
	RecordedOnElement *fhir.PrimitiveElement    `json:"-"`
	Source            *Reference                `json:"source,omitempty"`
	Device            *Reference                `json:"device,omitempty"`
	ReasonCode        []CodeableConcept         `json:"reasonCode,omitempty"`
	ReasonReference   []Reference               `json:"reasonReference,omitempty"`
	BodySite          *CodeableConcept          `json:"bodySite,omitempty"`
	Note              []Annotation              `json:"note,omitempty"`
}

// ResourceType returns the FHIR discriminator "DeviceUseStatement".
func (r *DeviceUseStatement) ResourceType() string { return DeviceUseStatementResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a DeviceUseStatement never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *DeviceUseStatement) MarshalJSON() ([]byte, error) {
	type alias DeviceUseStatement
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: DeviceUseStatementResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalDeviceUseStatementSiblings(r, encoded)
}

// marshalDeviceUseStatementSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDeviceUseStatementSiblings(v *DeviceUseStatement, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.RecordedOnElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_recordedOn", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through fhir.UnmarshalResource so the concrete type behind the interface is recovered;
// the standard codec cannot decode a resource object into an interface.
func (v *DeviceUseStatement) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_recordedOn"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.RecordedOnElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := fhir.UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DeviceUseStatement
	return json.Unmarshal(residual, (*alias)(v))
}

// DeviceUseStatementTiming is the sealed value interface for the timing[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isDeviceUseStatementTiming marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type DeviceUseStatementTiming interface{ isDeviceUseStatementTiming() }

func (Timing) isDeviceUseStatementTiming()       {}
func (Period) isDeviceUseStatementTiming()       {}
func (FHIRDateTime) isDeviceUseStatementTiming() {}

// Timing returns the value set in the timing[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *DeviceUseStatement) Timing() (DeviceUseStatementTiming, bool) {
	switch {
	case r.TimingTiming != nil:
		return *r.TimingTiming, true
	case r.TimingPeriod != nil:
		return *r.TimingPeriod, true
	case r.TimingDateTime != nil:
		return *r.TimingDateTime, true
	}
	return nil, false
}

// SetTimingTiming sets timing[x] to a Timing and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DeviceUseStatement) SetTimingTiming(v Timing) {
	r.TimingTiming = nil
	r.TimingPeriod = nil
	r.TimingDateTime = nil
	r.TimingTiming = &v
}

// SetTimingPeriod sets timing[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DeviceUseStatement) SetTimingPeriod(v Period) {
	r.TimingTiming = nil
	r.TimingPeriod = nil
	r.TimingDateTime = nil
	r.TimingPeriod = &v
}

// SetTimingDateTime sets timing[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isDeviceUseStatementTiming marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DeviceUseStatement) SetTimingDateTime(v FHIRDateTime) {
	r.TimingTiming = nil
	r.TimingPeriod = nil
	r.TimingDateTime = nil
	r.TimingDateTime = &v
}
