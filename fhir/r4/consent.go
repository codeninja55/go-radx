// Code generated by fhir-gen; DO NOT EDIT.

package r4

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// ConsentResourceType is the FHIR resourceType discriminator for Consent.
const ConsentResourceType = "Consent"

// Consent is the generated FHIR Consent resource.
type Consent struct {
	DomainResource
	Identifier       []Identifier           `json:"identifier,omitempty"`
	Status           *ConsentState          `json:"status,omitempty"`
	StatusElement    *fhir.PrimitiveElement `json:"-"`
	Scope            *CodeableConcept       `json:"scope,omitempty"`
	Category         []CodeableConcept      `json:"category,omitempty"`
	Patient          *Reference             `json:"patient,omitempty"`
	DateTime         *string                `json:"dateTime,omitempty"`
	DateTimeElement  *fhir.PrimitiveElement `json:"-"`
	Performer        []Reference            `json:"performer,omitempty"`
	Organization     []Reference            `json:"organization,omitempty"`
	SourceAttachment *Attachment            `json:"sourceAttachment,omitempty"`
	SourceReference  *Reference             `json:"sourceReference,omitempty"`
	Policy           []ConsentPolicy        `json:"policy,omitempty"`
	PolicyRule       *CodeableConcept       `json:"policyRule,omitempty"`
	Verification     []ConsentVerification  `json:"verification,omitempty"`
	Provision        *ConsentProvision      `json:"provision,omitempty"`
}

// ResourceType returns the FHIR discriminator "Consent".
func (r *Consent) ResourceType() string { return ConsentResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a Consent never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *Consent) MarshalJSON() ([]byte, error) {
	type alias Consent
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: ConsentResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalConsentSiblings(r, encoded)
}

// marshalConsentSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalConsentSiblings(v *Consent, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.DateTimeElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_dateTime", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through fhir.UnmarshalResource so the concrete type behind the interface is recovered;
// the standard codec cannot decode a resource object into an interface.
func (v *Consent) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_dateTime"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DateTimeElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := fhir.UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias Consent
	return json.Unmarshal(residual, (*alias)(v))
}

// ConsentSource is the sealed value interface for the source[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isConsentSource marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type ConsentSource interface{ isConsentSource() }

func (Attachment) isConsentSource() {}
func (Reference) isConsentSource()  {}

// Source returns the value set in the source[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *Consent) Source() (ConsentSource, bool) {
	switch {
	case r.SourceAttachment != nil:
		return *r.SourceAttachment, true
	case r.SourceReference != nil:
		return *r.SourceReference, true
	}
	return nil, false
}

// SetSourceAttachment sets source[x] to a Attachment and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *Consent) SetSourceAttachment(v Attachment) {
	r.SourceAttachment = nil
	r.SourceReference = nil
	r.SourceAttachment = &v
}

// SetSourceReference sets source[x] to a Reference and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *Consent) SetSourceReference(v Reference) {
	r.SourceAttachment = nil
	r.SourceReference = nil
	r.SourceReference = &v
}

// ConsentPolicy is a generated nested backbone element.
type ConsentPolicy struct {
	BackboneElement
	Authority        *string                `json:"authority,omitempty"`
	AuthorityElement *fhir.PrimitiveElement `json:"-"`
	URI              *string                `json:"uri,omitempty"`
	URIElement       *fhir.PrimitiveElement `json:"-"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v ConsentPolicy) MarshalJSON() ([]byte, error) {
	type alias ConsentPolicy
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalConsentPolicySiblings(&v, encoded)
}

// marshalConsentPolicySiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalConsentPolicySiblings(v *ConsentPolicy, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.AuthorityElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_authority", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.URIElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_uri", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *ConsentPolicy) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_authority"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.AuthorityElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_uri"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.URIElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias ConsentPolicy
	return json.Unmarshal(residual, (*alias)(v))
}

// ConsentProvision is a generated nested backbone element.
type ConsentProvision struct {
	BackboneElement
	Type          *ConsentProvisionType   `json:"type,omitempty"`
	TypeElement   *fhir.PrimitiveElement  `json:"-"`
	Period        *Period                 `json:"period,omitempty"`
	Actor         []ConsentProvisionActor `json:"actor,omitempty"`
	Action        []CodeableConcept       `json:"action,omitempty"`
	SecurityLabel []Coding                `json:"securityLabel,omitempty"`
	Purpose       []Coding                `json:"purpose,omitempty"`
	Class         []Coding                `json:"class,omitempty"`
	Code          []CodeableConcept       `json:"code,omitempty"`
	DataPeriod    *Period                 `json:"dataPeriod,omitempty"`
	Data          []ConsentProvisionData  `json:"data,omitempty"`
	Provision     []ConsentProvision      `json:"provision,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v ConsentProvision) MarshalJSON() ([]byte, error) {
	type alias ConsentProvision
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalConsentProvisionSiblings(&v, encoded)
}

// marshalConsentProvisionSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalConsentProvisionSiblings(v *ConsentProvision, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.TypeElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_type", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *ConsentProvision) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_type"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.TypeElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias ConsentProvision
	return json.Unmarshal(residual, (*alias)(v))
}

// ConsentProvisionActor is a generated nested backbone element.
type ConsentProvisionActor struct {
	BackboneElement
	Role      *CodeableConcept `json:"role,omitempty"`
	Reference *Reference       `json:"reference,omitempty"`
}

// ConsentProvisionData is a generated nested backbone element.
type ConsentProvisionData struct {
	BackboneElement
	Meaning        *ConsentDataMeaning    `json:"meaning,omitempty"`
	MeaningElement *fhir.PrimitiveElement `json:"-"`
	Reference      *Reference             `json:"reference,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v ConsentProvisionData) MarshalJSON() ([]byte, error) {
	type alias ConsentProvisionData
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalConsentProvisionDataSiblings(&v, encoded)
}

// marshalConsentProvisionDataSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalConsentProvisionDataSiblings(v *ConsentProvisionData, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.MeaningElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_meaning", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *ConsentProvisionData) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_meaning"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.MeaningElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias ConsentProvisionData
	return json.Unmarshal(residual, (*alias)(v))
}

// ConsentVerification is a generated nested backbone element.
type ConsentVerification struct {
	BackboneElement
	Verified                *bool                  `json:"verified,omitempty"`
	VerifiedElement         *fhir.PrimitiveElement `json:"-"`
	VerifiedWith            *Reference             `json:"verifiedWith,omitempty"`
	VerificationDate        *string                `json:"verificationDate,omitempty"`
	VerificationDateElement *fhir.PrimitiveElement `json:"-"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v ConsentVerification) MarshalJSON() ([]byte, error) {
	type alias ConsentVerification
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalConsentVerificationSiblings(&v, encoded)
}

// marshalConsentVerificationSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalConsentVerificationSiblings(v *ConsentVerification, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.VerifiedElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_verified", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.VerificationDateElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_verificationDate", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *ConsentVerification) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_verified"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.VerifiedElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_verificationDate"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.VerificationDateElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias ConsentVerification
	return json.Unmarshal(residual, (*alias)(v))
}
