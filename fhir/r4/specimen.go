// Code generated by fhir-gen; DO NOT EDIT.

package r4

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// SpecimenResourceType is the FHIR resourceType discriminator for Specimen.
const SpecimenResourceType = "Specimen"

// Specimen is the generated FHIR Specimen resource.
type Specimen struct {
	DomainResource
	Identifier          []Identifier           `json:"identifier,omitempty"`
	AccessionIdentifier *Identifier            `json:"accessionIdentifier,omitempty"`
	Status              *SpecimenStatus        `json:"status,omitempty"`
	StatusElement       *fhir.PrimitiveElement `json:"-"`
	Type                *CodeableConcept       `json:"type,omitempty"`
	Subject             *Reference             `json:"subject,omitempty"`
	ReceivedTime        *string                `json:"receivedTime,omitempty"`
	ReceivedTimeElement *fhir.PrimitiveElement `json:"-"`
	Parent              []Reference            `json:"parent,omitempty"`
	Request             []Reference            `json:"request,omitempty"`
	Collection          *SpecimenCollection    `json:"collection,omitempty"`
	Processing          []SpecimenProcessing   `json:"processing,omitempty"`
	Container           []SpecimenContainer    `json:"container,omitempty"`
	Condition           []CodeableConcept      `json:"condition,omitempty"`
	Note                []Annotation           `json:"note,omitempty"`
}

// ResourceType returns the FHIR discriminator "Specimen".
func (r *Specimen) ResourceType() string { return SpecimenResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a Specimen never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *Specimen) MarshalJSON() ([]byte, error) {
	type alias Specimen
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: SpecimenResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalSpecimenSiblings(r, encoded)
}

// marshalSpecimenSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalSpecimenSiblings(v *Specimen, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.ReceivedTimeElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_receivedTime", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through the release-local UnmarshalResource so the concrete type behind the interface
// is recovered as a resource of this release; the standard codec cannot decode a resource
// object into an interface.
func (v *Specimen) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_receivedTime"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ReceivedTimeElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias Specimen
	return json.Unmarshal(residual, (*alias)(v))
}

// SpecimenCollection is a generated nested backbone element.
type SpecimenCollection struct {
	BackboneElement
	Collector                    *Reference       `json:"collector,omitempty"`
	CollectedDateTime            *FHIRDateTime    `json:"collectedDateTime,omitempty"`
	CollectedPeriod              *Period          `json:"collectedPeriod,omitempty"`
	Duration                     *Duration        `json:"duration,omitempty"`
	Quantity                     *Quantity        `json:"quantity,omitempty"`
	Method                       *CodeableConcept `json:"method,omitempty"`
	BodySite                     *CodeableConcept `json:"bodySite,omitempty"`
	FastingStatusCodeableConcept *CodeableConcept `json:"fastingStatusCodeableConcept,omitempty"`
	FastingStatusDuration        *Duration        `json:"fastingStatusDuration,omitempty"`
}

// SpecimenCollectionCollected is the sealed value interface for the collected[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isSpecimenCollectionCollected marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type SpecimenCollectionCollected interface{ isSpecimenCollectionCollected() }

func (FHIRDateTime) isSpecimenCollectionCollected() {}
func (Period) isSpecimenCollectionCollected()       {}

// Collected returns the value set in the collected[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *SpecimenCollection) Collected() (SpecimenCollectionCollected, bool) {
	switch {
	case r.CollectedDateTime != nil:
		return *r.CollectedDateTime, true
	case r.CollectedPeriod != nil:
		return *r.CollectedPeriod, true
	}
	return nil, false
}

// SetCollectedDateTime sets collected[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isSpecimenCollectionCollected marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenCollection) SetCollectedDateTime(v FHIRDateTime) {
	r.CollectedDateTime = nil
	r.CollectedPeriod = nil
	r.CollectedDateTime = &v
}

// SetCollectedPeriod sets collected[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenCollection) SetCollectedPeriod(v Period) {
	r.CollectedDateTime = nil
	r.CollectedPeriod = nil
	r.CollectedPeriod = &v
}

// SpecimenCollectionFastingStatus is the sealed value interface for the fastingStatus[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isSpecimenCollectionFastingStatus marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type SpecimenCollectionFastingStatus interface{ isSpecimenCollectionFastingStatus() }

func (CodeableConcept) isSpecimenCollectionFastingStatus() {}
func (Duration) isSpecimenCollectionFastingStatus()        {}

// FastingStatus returns the value set in the fastingStatus[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *SpecimenCollection) FastingStatus() (SpecimenCollectionFastingStatus, bool) {
	switch {
	case r.FastingStatusCodeableConcept != nil:
		return *r.FastingStatusCodeableConcept, true
	case r.FastingStatusDuration != nil:
		return *r.FastingStatusDuration, true
	}
	return nil, false
}

// SetFastingStatusCodeableConcept sets fastingStatus[x] to a CodeableConcept and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenCollection) SetFastingStatusCodeableConcept(v CodeableConcept) {
	r.FastingStatusCodeableConcept = nil
	r.FastingStatusDuration = nil
	r.FastingStatusCodeableConcept = &v
}

// SetFastingStatusDuration sets fastingStatus[x] to a Duration and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenCollection) SetFastingStatusDuration(v Duration) {
	r.FastingStatusCodeableConcept = nil
	r.FastingStatusDuration = nil
	r.FastingStatusDuration = &v
}

// SpecimenContainer is a generated nested backbone element.
type SpecimenContainer struct {
	BackboneElement
	Identifier              []Identifier           `json:"identifier,omitempty"`
	Description             *string                `json:"description,omitempty"`
	DescriptionElement      *fhir.PrimitiveElement `json:"-"`
	Type                    *CodeableConcept       `json:"type,omitempty"`
	Capacity                *Quantity              `json:"capacity,omitempty"`
	SpecimenQuantity        *Quantity              `json:"specimenQuantity,omitempty"`
	AdditiveCodeableConcept *CodeableConcept       `json:"additiveCodeableConcept,omitempty"`
	AdditiveReference       *Reference             `json:"additiveReference,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v SpecimenContainer) MarshalJSON() ([]byte, error) {
	type alias SpecimenContainer
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalSpecimenContainerSiblings(&v, encoded)
}

// marshalSpecimenContainerSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalSpecimenContainerSiblings(v *SpecimenContainer, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.DescriptionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_description", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *SpecimenContainer) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_description"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DescriptionElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias SpecimenContainer
	return json.Unmarshal(residual, (*alias)(v))
}

// SpecimenContainerAdditive is the sealed value interface for the additive[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isSpecimenContainerAdditive marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type SpecimenContainerAdditive interface{ isSpecimenContainerAdditive() }

func (CodeableConcept) isSpecimenContainerAdditive() {}
func (Reference) isSpecimenContainerAdditive()       {}

// Additive returns the value set in the additive[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *SpecimenContainer) Additive() (SpecimenContainerAdditive, bool) {
	switch {
	case r.AdditiveCodeableConcept != nil:
		return *r.AdditiveCodeableConcept, true
	case r.AdditiveReference != nil:
		return *r.AdditiveReference, true
	}
	return nil, false
}

// SetAdditiveCodeableConcept sets additive[x] to a CodeableConcept and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenContainer) SetAdditiveCodeableConcept(v CodeableConcept) {
	r.AdditiveCodeableConcept = nil
	r.AdditiveReference = nil
	r.AdditiveCodeableConcept = &v
}

// SetAdditiveReference sets additive[x] to a Reference and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenContainer) SetAdditiveReference(v Reference) {
	r.AdditiveCodeableConcept = nil
	r.AdditiveReference = nil
	r.AdditiveReference = &v
}

// SpecimenProcessing is a generated nested backbone element.
type SpecimenProcessing struct {
	BackboneElement
	Description        *string                `json:"description,omitempty"`
	DescriptionElement *fhir.PrimitiveElement `json:"-"`
	Procedure          *CodeableConcept       `json:"procedure,omitempty"`
	Additive           []Reference            `json:"additive,omitempty"`
	TimeDateTime       *FHIRDateTime          `json:"timeDateTime,omitempty"`
	TimePeriod         *Period                `json:"timePeriod,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v SpecimenProcessing) MarshalJSON() ([]byte, error) {
	type alias SpecimenProcessing
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalSpecimenProcessingSiblings(&v, encoded)
}

// marshalSpecimenProcessingSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalSpecimenProcessingSiblings(v *SpecimenProcessing, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.DescriptionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_description", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *SpecimenProcessing) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_description"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DescriptionElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias SpecimenProcessing
	return json.Unmarshal(residual, (*alias)(v))
}

// SpecimenProcessingTime is the sealed value interface for the time[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isSpecimenProcessingTime marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type SpecimenProcessingTime interface{ isSpecimenProcessingTime() }

func (FHIRDateTime) isSpecimenProcessingTime() {}
func (Period) isSpecimenProcessingTime()       {}

// Time returns the value set in the time[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *SpecimenProcessing) Time() (SpecimenProcessingTime, bool) {
	switch {
	case r.TimeDateTime != nil:
		return *r.TimeDateTime, true
	case r.TimePeriod != nil:
		return *r.TimePeriod, true
	}
	return nil, false
}

// SetTimeDateTime sets time[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isSpecimenProcessingTime marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenProcessing) SetTimeDateTime(v FHIRDateTime) {
	r.TimeDateTime = nil
	r.TimePeriod = nil
	r.TimeDateTime = &v
}

// SetTimePeriod sets time[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SpecimenProcessing) SetTimePeriod(v Period) {
	r.TimeDateTime = nil
	r.TimePeriod = nil
	r.TimePeriod = &v
}
