// Code generated by fhir-gen; DO NOT EDIT.

package r4

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// BiologicallyDerivedProductResourceType is the FHIR resourceType discriminator for BiologicallyDerivedProduct.
const BiologicallyDerivedProductResourceType = "BiologicallyDerivedProduct"

// BiologicallyDerivedProduct is the generated FHIR BiologicallyDerivedProduct resource.
type BiologicallyDerivedProduct struct {
	DomainResource
	Identifier             []Identifier                            `json:"identifier,omitempty"`
	ProductCategory        *BiologicallyDerivedProductCategory     `json:"productCategory,omitempty"`
	ProductCategoryElement *fhir.PrimitiveElement                  `json:"-"`
	ProductCode            *CodeableConcept                        `json:"productCode,omitempty"`
	Status                 *BiologicallyDerivedProductStatus       `json:"status,omitempty"`
	StatusElement          *fhir.PrimitiveElement                  `json:"-"`
	Request                []Reference                             `json:"request,omitempty"`
	Quantity               *int32                                  `json:"quantity,omitempty"`
	QuantityElement        *fhir.PrimitiveElement                  `json:"-"`
	Parent                 []Reference                             `json:"parent,omitempty"`
	Collection             *BiologicallyDerivedProductCollection   `json:"collection,omitempty"`
	Processing             []BiologicallyDerivedProductProcessing  `json:"processing,omitempty"`
	Manipulation           *BiologicallyDerivedProductManipulation `json:"manipulation,omitempty"`
	Storage                []BiologicallyDerivedProductStorage     `json:"storage,omitempty"`
}

// ResourceType returns the FHIR discriminator "BiologicallyDerivedProduct".
func (r *BiologicallyDerivedProduct) ResourceType() string {
	return BiologicallyDerivedProductResourceType
}

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a BiologicallyDerivedProduct never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *BiologicallyDerivedProduct) MarshalJSON() ([]byte, error) {
	type alias BiologicallyDerivedProduct
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: BiologicallyDerivedProductResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalBiologicallyDerivedProductSiblings(r, encoded)
}

// marshalBiologicallyDerivedProductSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalBiologicallyDerivedProductSiblings(v *BiologicallyDerivedProduct, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.ProductCategoryElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_productCategory", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.QuantityElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_quantity", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through fhir.UnmarshalResource so the concrete type behind the interface is recovered;
// the standard codec cannot decode a resource object into an interface.
func (v *BiologicallyDerivedProduct) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_productCategory"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ProductCategoryElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_quantity"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.QuantityElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := fhir.UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias BiologicallyDerivedProduct
	return json.Unmarshal(residual, (*alias)(v))
}

// BiologicallyDerivedProductCollection is a generated nested backbone element.
type BiologicallyDerivedProductCollection struct {
	BackboneElement
	Collector         *Reference    `json:"collector,omitempty"`
	Source            *Reference    `json:"source,omitempty"`
	CollectedDateTime *FHIRDateTime `json:"collectedDateTime,omitempty"`
	CollectedPeriod   *Period       `json:"collectedPeriod,omitempty"`
}

// BiologicallyDerivedProductCollectionCollected is the sealed value interface for the collected[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isBiologicallyDerivedProductCollectionCollected marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type BiologicallyDerivedProductCollectionCollected interface{ isBiologicallyDerivedProductCollectionCollected() }

func (FHIRDateTime) isBiologicallyDerivedProductCollectionCollected() {}
func (Period) isBiologicallyDerivedProductCollectionCollected()       {}

// Collected returns the value set in the collected[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *BiologicallyDerivedProductCollection) Collected() (BiologicallyDerivedProductCollectionCollected, bool) {
	switch {
	case r.CollectedDateTime != nil:
		return *r.CollectedDateTime, true
	case r.CollectedPeriod != nil:
		return *r.CollectedPeriod, true
	}
	return nil, false
}

// SetCollectedDateTime sets collected[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isBiologicallyDerivedProductCollectionCollected marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *BiologicallyDerivedProductCollection) SetCollectedDateTime(v FHIRDateTime) {
	r.CollectedDateTime = nil
	r.CollectedPeriod = nil
	r.CollectedDateTime = &v
}

// SetCollectedPeriod sets collected[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *BiologicallyDerivedProductCollection) SetCollectedPeriod(v Period) {
	r.CollectedDateTime = nil
	r.CollectedPeriod = nil
	r.CollectedPeriod = &v
}

// BiologicallyDerivedProductManipulation is a generated nested backbone element.
type BiologicallyDerivedProductManipulation struct {
	BackboneElement
	Description        *string                `json:"description,omitempty"`
	DescriptionElement *fhir.PrimitiveElement `json:"-"`
	TimeDateTime       *FHIRDateTime          `json:"timeDateTime,omitempty"`
	TimePeriod         *Period                `json:"timePeriod,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v BiologicallyDerivedProductManipulation) MarshalJSON() ([]byte, error) {
	type alias BiologicallyDerivedProductManipulation
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalBiologicallyDerivedProductManipulationSiblings(&v, encoded)
}

// marshalBiologicallyDerivedProductManipulationSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalBiologicallyDerivedProductManipulationSiblings(v *BiologicallyDerivedProductManipulation, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.DescriptionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_description", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *BiologicallyDerivedProductManipulation) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_description"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DescriptionElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias BiologicallyDerivedProductManipulation
	return json.Unmarshal(residual, (*alias)(v))
}

// BiologicallyDerivedProductManipulationTime is the sealed value interface for the time[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isBiologicallyDerivedProductManipulationTime marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type BiologicallyDerivedProductManipulationTime interface{ isBiologicallyDerivedProductManipulationTime() }

func (FHIRDateTime) isBiologicallyDerivedProductManipulationTime() {}
func (Period) isBiologicallyDerivedProductManipulationTime()       {}

// Time returns the value set in the time[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *BiologicallyDerivedProductManipulation) Time() (BiologicallyDerivedProductManipulationTime, bool) {
	switch {
	case r.TimeDateTime != nil:
		return *r.TimeDateTime, true
	case r.TimePeriod != nil:
		return *r.TimePeriod, true
	}
	return nil, false
}

// SetTimeDateTime sets time[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isBiologicallyDerivedProductManipulationTime marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *BiologicallyDerivedProductManipulation) SetTimeDateTime(v FHIRDateTime) {
	r.TimeDateTime = nil
	r.TimePeriod = nil
	r.TimeDateTime = &v
}

// SetTimePeriod sets time[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *BiologicallyDerivedProductManipulation) SetTimePeriod(v Period) {
	r.TimeDateTime = nil
	r.TimePeriod = nil
	r.TimePeriod = &v
}

// BiologicallyDerivedProductProcessing is a generated nested backbone element.
type BiologicallyDerivedProductProcessing struct {
	BackboneElement
	Description        *string                `json:"description,omitempty"`
	DescriptionElement *fhir.PrimitiveElement `json:"-"`
	Procedure          *CodeableConcept       `json:"procedure,omitempty"`
	Additive           *Reference             `json:"additive,omitempty"`
	TimeDateTime       *FHIRDateTime          `json:"timeDateTime,omitempty"`
	TimePeriod         *Period                `json:"timePeriod,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v BiologicallyDerivedProductProcessing) MarshalJSON() ([]byte, error) {
	type alias BiologicallyDerivedProductProcessing
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalBiologicallyDerivedProductProcessingSiblings(&v, encoded)
}

// marshalBiologicallyDerivedProductProcessingSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalBiologicallyDerivedProductProcessingSiblings(v *BiologicallyDerivedProductProcessing, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.DescriptionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_description", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *BiologicallyDerivedProductProcessing) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_description"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DescriptionElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias BiologicallyDerivedProductProcessing
	return json.Unmarshal(residual, (*alias)(v))
}

// BiologicallyDerivedProductProcessingTime is the sealed value interface for the time[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isBiologicallyDerivedProductProcessingTime marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type BiologicallyDerivedProductProcessingTime interface{ isBiologicallyDerivedProductProcessingTime() }

func (FHIRDateTime) isBiologicallyDerivedProductProcessingTime() {}
func (Period) isBiologicallyDerivedProductProcessingTime()       {}

// Time returns the value set in the time[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *BiologicallyDerivedProductProcessing) Time() (BiologicallyDerivedProductProcessingTime, bool) {
	switch {
	case r.TimeDateTime != nil:
		return *r.TimeDateTime, true
	case r.TimePeriod != nil:
		return *r.TimePeriod, true
	}
	return nil, false
}

// SetTimeDateTime sets time[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isBiologicallyDerivedProductProcessingTime marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *BiologicallyDerivedProductProcessing) SetTimeDateTime(v FHIRDateTime) {
	r.TimeDateTime = nil
	r.TimePeriod = nil
	r.TimeDateTime = &v
}

// SetTimePeriod sets time[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *BiologicallyDerivedProductProcessing) SetTimePeriod(v Period) {
	r.TimeDateTime = nil
	r.TimePeriod = nil
	r.TimePeriod = &v
}

// BiologicallyDerivedProductStorage is a generated nested backbone element.
type BiologicallyDerivedProductStorage struct {
	BackboneElement
	Description        *string                                 `json:"description,omitempty"`
	DescriptionElement *fhir.PrimitiveElement                  `json:"-"`
	Temperature        *fhir.Decimal                           `json:"temperature,omitempty"`
	TemperatureElement *fhir.PrimitiveElement                  `json:"-"`
	Scale              *BiologicallyDerivedProductStorageScale `json:"scale,omitempty"`
	ScaleElement       *fhir.PrimitiveElement                  `json:"-"`
	Duration           *Period                                 `json:"duration,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v BiologicallyDerivedProductStorage) MarshalJSON() ([]byte, error) {
	type alias BiologicallyDerivedProductStorage
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalBiologicallyDerivedProductStorageSiblings(&v, encoded)
}

// marshalBiologicallyDerivedProductStorageSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalBiologicallyDerivedProductStorageSiblings(v *BiologicallyDerivedProductStorage, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.DescriptionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_description", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.TemperatureElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_temperature", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.ScaleElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_scale", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *BiologicallyDerivedProductStorage) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_description"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DescriptionElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_temperature"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.TemperatureElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_scale"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ScaleElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias BiologicallyDerivedProductStorage
	return json.Unmarshal(residual, (*alias)(v))
}
