// Code generated by fhir-gen; DO NOT EDIT.

package r4

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// SubstanceAmount is the generated FHIR SubstanceAmount datatype.
type SubstanceAmount struct {
	BackboneElement
	AmountQuantity    *Quantity                      `json:"amountQuantity,omitempty"`
	AmountRange       *Range                         `json:"amountRange,omitempty"`
	AmountString      *FHIRString                    `json:"amountString,omitempty"`
	AmountType        *CodeableConcept               `json:"amountType,omitempty"`
	AmountText        *string                        `json:"amountText,omitempty"`
	AmountTextElement *fhir.PrimitiveElement         `json:"-"`
	ReferenceRange    *SubstanceAmountReferenceRange `json:"referenceRange,omitempty"`
}

// MarshalJSON folds the primitive "_field" siblings into the encoded value: a scalar
// sibling is dropped when it carries no id or extension, and a repeating sibling's
// array is null-aligned with its value array, so a partially-extended repeating
// primitive lines up position-for-position on the wire (a position with no
// id/extension becomes a JSON null in the "_field" array).
func (v SubstanceAmount) MarshalJSON() ([]byte, error) {
	type alias SubstanceAmount
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalSubstanceAmountSiblings(&v, encoded)
}

// marshalSubstanceAmountSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalSubstanceAmountSiblings(v *SubstanceAmount, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.AmountTextElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_amountText", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *SubstanceAmount) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_amountText"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.AmountTextElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias SubstanceAmount
	return json.Unmarshal(residual, (*alias)(v))
}

// SubstanceAmountAmount is the sealed value interface for the amount[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isSubstanceAmountAmount marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type SubstanceAmountAmount interface{ isSubstanceAmountAmount() }

func (Quantity) isSubstanceAmountAmount()   {}
func (Range) isSubstanceAmountAmount()      {}
func (FHIRString) isSubstanceAmountAmount() {}

// Amount returns the value set in the amount[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *SubstanceAmount) Amount() (SubstanceAmountAmount, bool) {
	switch {
	case r.AmountQuantity != nil:
		return *r.AmountQuantity, true
	case r.AmountRange != nil:
		return *r.AmountRange, true
	case r.AmountString != nil:
		return *r.AmountString, true
	}
	return nil, false
}

// SetAmountQuantity sets amount[x] to a Quantity and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SubstanceAmount) SetAmountQuantity(v Quantity) {
	r.AmountQuantity = nil
	r.AmountRange = nil
	r.AmountString = nil
	r.AmountQuantity = &v
}

// SetAmountRange sets amount[x] to a Range and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SubstanceAmount) SetAmountRange(v Range) {
	r.AmountQuantity = nil
	r.AmountRange = nil
	r.AmountString = nil
	r.AmountRange = &v
}

// SetAmountString sets amount[x] to a FHIRString (the
// release primitive wrapper that carries the isSubstanceAmountAmount marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SubstanceAmount) SetAmountString(v FHIRString) {
	r.AmountQuantity = nil
	r.AmountRange = nil
	r.AmountString = nil
	r.AmountString = &v
}

// SubstanceAmountReferenceRange is a generated nested backbone element.
type SubstanceAmountReferenceRange struct {
	Element
	LowLimit  *Quantity `json:"lowLimit,omitempty"`
	HighLimit *Quantity `json:"highLimit,omitempty"`
}
