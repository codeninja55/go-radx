// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// DetectedIssueResourceType is the FHIR resourceType discriminator for DetectedIssue.
const DetectedIssueResourceType = "DetectedIssue"

// DetectedIssue is the generated FHIR DetectedIssue resource.
type DetectedIssue struct {
	DomainResource
	Identifier         []Identifier              `json:"identifier,omitempty"`
	Status             *DetectedIssueStatus      `json:"status,omitempty"`
	StatusElement      *fhir.PrimitiveElement    `json:"-"`
	Category           []CodeableConcept         `json:"category,omitempty"`
	Code               *CodeableConcept          `json:"code,omitempty"`
	Severity           *DetectedIssueSeverity    `json:"severity,omitempty"`
	SeverityElement    *fhir.PrimitiveElement    `json:"-"`
	Subject            *Reference                `json:"subject,omitempty"`
	Encounter          *Reference                `json:"encounter,omitempty"`
	IdentifiedDateTime *FHIRDateTime             `json:"identifiedDateTime,omitempty"`
	IdentifiedPeriod   *Period                   `json:"identifiedPeriod,omitempty"`
	Author             *Reference                `json:"author,omitempty"`
	Implicated         []Reference               `json:"implicated,omitempty"`
	Evidence           []DetectedIssueEvidence   `json:"evidence,omitempty"`
	Detail             *string                   `json:"detail,omitempty"`
	DetailElement      *fhir.PrimitiveElement    `json:"-"`
	Reference          *string                   `json:"reference,omitempty"`
	ReferenceElement   *fhir.PrimitiveElement    `json:"-"`
	Mitigation         []DetectedIssueMitigation `json:"mitigation,omitempty"`
}

// ResourceType returns the FHIR discriminator "DetectedIssue".
func (r *DetectedIssue) ResourceType() string { return DetectedIssueResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a DetectedIssue never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *DetectedIssue) MarshalJSON() ([]byte, error) {
	type alias DetectedIssue
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: DetectedIssueResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalDetectedIssueSiblings(r, encoded)
}

// marshalDetectedIssueSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDetectedIssueSiblings(v *DetectedIssue, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.SeverityElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_severity", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.DetailElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_detail", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.ReferenceElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_reference", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through the release-local UnmarshalResource so the concrete type behind the interface
// is recovered as a resource of this release; the standard codec cannot decode a resource
// object into an interface.
func (v *DetectedIssue) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_severity"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.SeverityElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_detail"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DetailElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_reference"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ReferenceElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DetectedIssue
	return json.Unmarshal(residual, (*alias)(v))
}

// DetectedIssueIdentified is the sealed value interface for the identified[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isDetectedIssueIdentified marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type DetectedIssueIdentified interface{ isDetectedIssueIdentified() }

func (FHIRDateTime) isDetectedIssueIdentified() {}
func (Period) isDetectedIssueIdentified()       {}

// Identified returns the value set in the identified[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *DetectedIssue) Identified() (DetectedIssueIdentified, bool) {
	switch {
	case r.IdentifiedDateTime != nil:
		return *r.IdentifiedDateTime, true
	case r.IdentifiedPeriod != nil:
		return *r.IdentifiedPeriod, true
	}
	return nil, false
}

// SetIdentifiedDateTime sets identified[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isDetectedIssueIdentified marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DetectedIssue) SetIdentifiedDateTime(v FHIRDateTime) {
	r.IdentifiedDateTime = nil
	r.IdentifiedPeriod = nil
	r.IdentifiedDateTime = &v
}

// SetIdentifiedPeriod sets identified[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DetectedIssue) SetIdentifiedPeriod(v Period) {
	r.IdentifiedDateTime = nil
	r.IdentifiedPeriod = nil
	r.IdentifiedPeriod = &v
}

// DetectedIssueEvidence is a generated nested backbone element.
type DetectedIssueEvidence struct {
	BackboneElement
	Code   []CodeableConcept `json:"code,omitempty"`
	Detail []Reference       `json:"detail,omitempty"`
}

// DetectedIssueMitigation is a generated nested backbone element.
type DetectedIssueMitigation struct {
	BackboneElement
	Action      *CodeableConcept       `json:"action,omitempty"`
	Date        *string                `json:"date,omitempty"`
	DateElement *fhir.PrimitiveElement `json:"-"`
	Author      *Reference             `json:"author,omitempty"`
	Note        []Annotation           `json:"note,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v DetectedIssueMitigation) MarshalJSON() ([]byte, error) {
	type alias DetectedIssueMitigation
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalDetectedIssueMitigationSiblings(&v, encoded)
}

// marshalDetectedIssueMitigationSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDetectedIssueMitigationSiblings(v *DetectedIssueMitigation, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.DateElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_date", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *DetectedIssueMitigation) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_date"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DateElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DetectedIssueMitigation
	return json.Unmarshal(residual, (*alias)(v))
}
