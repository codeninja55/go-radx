// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// SubstanceResourceType is the FHIR resourceType discriminator for Substance.
const SubstanceResourceType = "Substance"

// Substance is the generated FHIR Substance resource.
type Substance struct {
	DomainResource
	Identifier         []Identifier           `json:"identifier,omitempty"`
	Instance           *bool                  `json:"instance,omitempty"`
	InstanceElement    *fhir.PrimitiveElement `json:"-"`
	Status             *FHIRSubstanceStatus   `json:"status,omitempty"`
	StatusElement      *fhir.PrimitiveElement `json:"-"`
	Category           []CodeableConcept      `json:"category,omitempty"`
	Code               *CodeableReference     `json:"code,omitempty"`
	Description        *string                `json:"description,omitempty"`
	DescriptionElement *fhir.PrimitiveElement `json:"-"`
	Expiry             *string                `json:"expiry,omitempty"`
	ExpiryElement      *fhir.PrimitiveElement `json:"-"`
	Quantity           *Quantity              `json:"quantity,omitempty"`
	Ingredient         []SubstanceIngredient  `json:"ingredient,omitempty"`
}

// ResourceType returns the FHIR discriminator "Substance".
func (r *Substance) ResourceType() string { return SubstanceResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a Substance never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *Substance) MarshalJSON() ([]byte, error) {
	type alias Substance
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: SubstanceResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalSubstanceSiblings(r, encoded)
}

// marshalSubstanceSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalSubstanceSiblings(v *Substance, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.InstanceElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_instance", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.DescriptionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_description", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.ExpiryElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_expiry", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through fhir.UnmarshalResource so the concrete type behind the interface is recovered;
// the standard codec cannot decode a resource object into an interface.
func (v *Substance) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_instance"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.InstanceElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_description"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DescriptionElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_expiry"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ExpiryElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := fhir.UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias Substance
	return json.Unmarshal(residual, (*alias)(v))
}

// SubstanceIngredient is a generated nested backbone element.
type SubstanceIngredient struct {
	BackboneElement
	Quantity                 *Ratio           `json:"quantity,omitempty"`
	SubstanceCodeableConcept *CodeableConcept `json:"substanceCodeableConcept,omitempty"`
	SubstanceReference       *Reference       `json:"substanceReference,omitempty"`
}

// SubstanceIngredientSubstance is the sealed value interface for the substance[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isSubstanceIngredientSubstance marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type SubstanceIngredientSubstance interface{ isSubstanceIngredientSubstance() }

func (CodeableConcept) isSubstanceIngredientSubstance() {}
func (Reference) isSubstanceIngredientSubstance()       {}

// Substance returns the value set in the substance[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *SubstanceIngredient) Substance() (SubstanceIngredientSubstance, bool) {
	switch {
	case r.SubstanceCodeableConcept != nil:
		return *r.SubstanceCodeableConcept, true
	case r.SubstanceReference != nil:
		return *r.SubstanceReference, true
	}
	return nil, false
}

// SetSubstanceCodeableConcept sets substance[x] to a CodeableConcept and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SubstanceIngredient) SetSubstanceCodeableConcept(v CodeableConcept) {
	r.SubstanceCodeableConcept = nil
	r.SubstanceReference = nil
	r.SubstanceCodeableConcept = &v
}

// SetSubstanceReference sets substance[x] to a Reference and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *SubstanceIngredient) SetSubstanceReference(v Reference) {
	r.SubstanceCodeableConcept = nil
	r.SubstanceReference = nil
	r.SubstanceReference = &v
}
