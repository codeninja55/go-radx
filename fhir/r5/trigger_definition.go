// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// TriggerDefinition is the generated FHIR TriggerDefinition datatype.
type TriggerDefinition struct {
	Element
	Type                     *TriggerType           `json:"type,omitempty"`
	TypeElement              *fhir.PrimitiveElement `json:"-"`
	Name                     *string                `json:"name,omitempty"`
	NameElement              *fhir.PrimitiveElement `json:"-"`
	Code                     *CodeableConcept       `json:"code,omitempty"`
	SubscriptionTopic        *string                `json:"subscriptionTopic,omitempty"`
	SubscriptionTopicElement *fhir.PrimitiveElement `json:"-"`
	TimingTiming             *Timing                `json:"timingTiming,omitempty"`
	TimingReference          *Reference             `json:"timingReference,omitempty"`
	TimingDate               *FHIRDate              `json:"timingDate,omitempty"`
	TimingDateTime           *FHIRDateTime          `json:"timingDateTime,omitempty"`
	Data                     []DataRequirement      `json:"data,omitempty"`
	Condition                *Expression            `json:"condition,omitempty"`
}

// MarshalJSON folds the primitive "_field" siblings into the encoded value: a scalar
// sibling is dropped when it carries no id or extension, and a repeating sibling's
// array is null-aligned with its value array, so a partially-extended repeating
// primitive lines up position-for-position on the wire (a position with no
// id/extension becomes a JSON null in the "_field" array).
func (v TriggerDefinition) MarshalJSON() ([]byte, error) {
	type alias TriggerDefinition
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalTriggerDefinitionSiblings(&v, encoded)
}

// marshalTriggerDefinitionSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalTriggerDefinitionSiblings(v *TriggerDefinition, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.TypeElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_type", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.NameElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_name", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.SubscriptionTopicElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_subscriptionTopic", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *TriggerDefinition) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_type"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.TypeElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_name"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.NameElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_subscriptionTopic"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.SubscriptionTopicElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias TriggerDefinition
	return json.Unmarshal(residual, (*alias)(v))
}

// TriggerDefinitionTiming is the sealed value interface for the timing[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isTriggerDefinitionTiming marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type TriggerDefinitionTiming interface{ isTriggerDefinitionTiming() }

func (Timing) isTriggerDefinitionTiming()       {}
func (Reference) isTriggerDefinitionTiming()    {}
func (FHIRDate) isTriggerDefinitionTiming()     {}
func (FHIRDateTime) isTriggerDefinitionTiming() {}

// Timing returns the value set in the timing[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *TriggerDefinition) Timing() (TriggerDefinitionTiming, bool) {
	switch {
	case r.TimingTiming != nil:
		return *r.TimingTiming, true
	case r.TimingReference != nil:
		return *r.TimingReference, true
	case r.TimingDate != nil:
		return *r.TimingDate, true
	case r.TimingDateTime != nil:
		return *r.TimingDateTime, true
	}
	return nil, false
}

// SetTimingTiming sets timing[x] to a Timing and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *TriggerDefinition) SetTimingTiming(v Timing) {
	r.TimingTiming = nil
	r.TimingReference = nil
	r.TimingDate = nil
	r.TimingDateTime = nil
	r.TimingTiming = &v
}

// SetTimingReference sets timing[x] to a Reference and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *TriggerDefinition) SetTimingReference(v Reference) {
	r.TimingTiming = nil
	r.TimingReference = nil
	r.TimingDate = nil
	r.TimingDateTime = nil
	r.TimingReference = &v
}

// SetTimingDate sets timing[x] to a FHIRDate (the
// release primitive wrapper that carries the isTriggerDefinitionTiming marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *TriggerDefinition) SetTimingDate(v FHIRDate) {
	r.TimingTiming = nil
	r.TimingReference = nil
	r.TimingDate = nil
	r.TimingDateTime = nil
	r.TimingDate = &v
}

// SetTimingDateTime sets timing[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isTriggerDefinitionTiming marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *TriggerDefinition) SetTimingDateTime(v FHIRDateTime) {
	r.TimingTiming = nil
	r.TimingReference = nil
	r.TimingDate = nil
	r.TimingDateTime = nil
	r.TimingDateTime = &v
}
