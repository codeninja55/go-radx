// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// MedicationAdministrationResourceType is the FHIR resourceType discriminator for MedicationAdministration.
const MedicationAdministrationResourceType = "MedicationAdministration"

// MedicationAdministration is the generated FHIR MedicationAdministration resource.
type MedicationAdministration struct {
	DomainResource
	Identifier            []Identifier                         `json:"identifier,omitempty"`
	BasedOn               []Reference                          `json:"basedOn,omitempty"`
	PartOf                []Reference                          `json:"partOf,omitempty"`
	Status                *MedicationAdministrationStatusCodes `json:"status,omitempty"`
	StatusElement         *fhir.PrimitiveElement               `json:"-"`
	StatusReason          []CodeableConcept                    `json:"statusReason,omitempty"`
	Category              []CodeableConcept                    `json:"category,omitempty"`
	Medication            *CodeableReference                   `json:"medication,omitempty"`
	Subject               *Reference                           `json:"subject,omitempty"`
	Encounter             *Reference                           `json:"encounter,omitempty"`
	SupportingInformation []Reference                          `json:"supportingInformation,omitempty"`
	OccurenceDateTime     *FHIRDateTime                        `json:"occurenceDateTime,omitempty"`
	OccurencePeriod       *Period                              `json:"occurencePeriod,omitempty"`
	OccurenceTiming       *Timing                              `json:"occurenceTiming,omitempty"`
	Recorded              *string                              `json:"recorded,omitempty"`
	RecordedElement       *fhir.PrimitiveElement               `json:"-"`
	IsSubPotent           *bool                                `json:"isSubPotent,omitempty"`
	IsSubPotentElement    *fhir.PrimitiveElement               `json:"-"`
	SubPotentReason       []CodeableConcept                    `json:"subPotentReason,omitempty"`
	Performer             []MedicationAdministrationPerformer  `json:"performer,omitempty"`
	Reason                []CodeableReference                  `json:"reason,omitempty"`
	Request               *Reference                           `json:"request,omitempty"`
	Device                []CodeableReference                  `json:"device,omitempty"`
	Note                  []Annotation                         `json:"note,omitempty"`
	Dosage                *MedicationAdministrationDosage      `json:"dosage,omitempty"`
	EventHistory          []Reference                          `json:"eventHistory,omitempty"`
}

// ResourceType returns the FHIR discriminator "MedicationAdministration".
func (r *MedicationAdministration) ResourceType() string { return MedicationAdministrationResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a MedicationAdministration never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *MedicationAdministration) MarshalJSON() ([]byte, error) {
	type alias MedicationAdministration
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: MedicationAdministrationResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalMedicationAdministrationSiblings(r, encoded)
}

// marshalMedicationAdministrationSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalMedicationAdministrationSiblings(v *MedicationAdministration, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.RecordedElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_recorded", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.IsSubPotentElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_isSubPotent", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through fhir.UnmarshalResource so the concrete type behind the interface is recovered;
// the standard codec cannot decode a resource object into an interface.
func (v *MedicationAdministration) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_recorded"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.RecordedElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_isSubPotent"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.IsSubPotentElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := fhir.UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias MedicationAdministration
	return json.Unmarshal(residual, (*alias)(v))
}

// MedicationAdministrationOccurence is the sealed value interface for the occurence[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isMedicationAdministrationOccurence marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type MedicationAdministrationOccurence interface{ isMedicationAdministrationOccurence() }

func (FHIRDateTime) isMedicationAdministrationOccurence() {}
func (Period) isMedicationAdministrationOccurence()       {}
func (Timing) isMedicationAdministrationOccurence()       {}

// Occurence returns the value set in the occurence[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *MedicationAdministration) Occurence() (MedicationAdministrationOccurence, bool) {
	switch {
	case r.OccurenceDateTime != nil:
		return *r.OccurenceDateTime, true
	case r.OccurencePeriod != nil:
		return *r.OccurencePeriod, true
	case r.OccurenceTiming != nil:
		return *r.OccurenceTiming, true
	}
	return nil, false
}

// SetOccurenceDateTime sets occurence[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isMedicationAdministrationOccurence marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *MedicationAdministration) SetOccurenceDateTime(v FHIRDateTime) {
	r.OccurenceDateTime = nil
	r.OccurencePeriod = nil
	r.OccurenceTiming = nil
	r.OccurenceDateTime = &v
}

// SetOccurencePeriod sets occurence[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *MedicationAdministration) SetOccurencePeriod(v Period) {
	r.OccurenceDateTime = nil
	r.OccurencePeriod = nil
	r.OccurenceTiming = nil
	r.OccurencePeriod = &v
}

// SetOccurenceTiming sets occurence[x] to a Timing and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *MedicationAdministration) SetOccurenceTiming(v Timing) {
	r.OccurenceDateTime = nil
	r.OccurencePeriod = nil
	r.OccurenceTiming = nil
	r.OccurenceTiming = &v
}

// MedicationAdministrationDosage is a generated nested backbone element.
type MedicationAdministrationDosage struct {
	BackboneElement
	Text         *string                `json:"text,omitempty"`
	TextElement  *fhir.PrimitiveElement `json:"-"`
	Site         *CodeableConcept       `json:"site,omitempty"`
	Route        *CodeableConcept       `json:"route,omitempty"`
	Method       *CodeableConcept       `json:"method,omitempty"`
	Dose         *Quantity              `json:"dose,omitempty"`
	RateRatio    *Ratio                 `json:"rateRatio,omitempty"`
	RateQuantity *Quantity              `json:"rateQuantity,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v MedicationAdministrationDosage) MarshalJSON() ([]byte, error) {
	type alias MedicationAdministrationDosage
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalMedicationAdministrationDosageSiblings(&v, encoded)
}

// marshalMedicationAdministrationDosageSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalMedicationAdministrationDosageSiblings(v *MedicationAdministrationDosage, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.TextElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_text", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *MedicationAdministrationDosage) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_text"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.TextElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias MedicationAdministrationDosage
	return json.Unmarshal(residual, (*alias)(v))
}

// MedicationAdministrationDosageRate is the sealed value interface for the rate[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isMedicationAdministrationDosageRate marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type MedicationAdministrationDosageRate interface{ isMedicationAdministrationDosageRate() }

func (Ratio) isMedicationAdministrationDosageRate()    {}
func (Quantity) isMedicationAdministrationDosageRate() {}

// Rate returns the value set in the rate[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *MedicationAdministrationDosage) Rate() (MedicationAdministrationDosageRate, bool) {
	switch {
	case r.RateRatio != nil:
		return *r.RateRatio, true
	case r.RateQuantity != nil:
		return *r.RateQuantity, true
	}
	return nil, false
}

// SetRateRatio sets rate[x] to a Ratio and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *MedicationAdministrationDosage) SetRateRatio(v Ratio) {
	r.RateRatio = nil
	r.RateQuantity = nil
	r.RateRatio = &v
}

// SetRateQuantity sets rate[x] to a Quantity and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *MedicationAdministrationDosage) SetRateQuantity(v Quantity) {
	r.RateRatio = nil
	r.RateQuantity = nil
	r.RateQuantity = &v
}

// MedicationAdministrationPerformer is a generated nested backbone element.
type MedicationAdministrationPerformer struct {
	BackboneElement
	Function *CodeableConcept   `json:"function,omitempty"`
	Actor    *CodeableReference `json:"actor,omitempty"`
}
