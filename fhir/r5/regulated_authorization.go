// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// RegulatedAuthorizationResourceType is the FHIR resourceType discriminator for RegulatedAuthorization.
const RegulatedAuthorizationResourceType = "RegulatedAuthorization"

// RegulatedAuthorization is the generated FHIR RegulatedAuthorization resource.
type RegulatedAuthorization struct {
	DomainResource
	Identifier         []Identifier                `json:"identifier,omitempty"`
	Subject            []Reference                 `json:"subject,omitempty"`
	Type               *CodeableConcept            `json:"type,omitempty"`
	Description        *string                     `json:"description,omitempty"`
	DescriptionElement *fhir.PrimitiveElement      `json:"-"`
	Region             []CodeableConcept           `json:"region,omitempty"`
	Status             *CodeableConcept            `json:"status,omitempty"`
	StatusDate         *string                     `json:"statusDate,omitempty"`
	StatusDateElement  *fhir.PrimitiveElement      `json:"-"`
	ValidityPeriod     *Period                     `json:"validityPeriod,omitempty"`
	Indication         []CodeableReference         `json:"indication,omitempty"`
	IntendedUse        *CodeableConcept            `json:"intendedUse,omitempty"`
	Basis              []CodeableConcept           `json:"basis,omitempty"`
	Holder             *Reference                  `json:"holder,omitempty"`
	Regulator          *Reference                  `json:"regulator,omitempty"`
	AttachedDocument   []Reference                 `json:"attachedDocument,omitempty"`
	Case               *RegulatedAuthorizationCase `json:"case,omitempty"`
}

// ResourceType returns the FHIR discriminator "RegulatedAuthorization".
func (r *RegulatedAuthorization) ResourceType() string { return RegulatedAuthorizationResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a RegulatedAuthorization never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *RegulatedAuthorization) MarshalJSON() ([]byte, error) {
	type alias RegulatedAuthorization
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: RegulatedAuthorizationResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalRegulatedAuthorizationSiblings(r, encoded)
}

// marshalRegulatedAuthorizationSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalRegulatedAuthorizationSiblings(v *RegulatedAuthorization, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.DescriptionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_description", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusDateElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_statusDate", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling and resource-typed field out of
// the object, decodes each into its companion field, then decodes the remaining keys
// into the value struct. A resource-typed field (the fhir.Resource interface) is decoded
// through fhir.UnmarshalResource so the concrete type behind the interface is recovered;
// the standard codec cannot decode a resource object into an interface.
func (v *RegulatedAuthorization) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_description"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DescriptionElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_statusDate"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusDateElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "contained"); ok {
		resources, err := fhir.UnmarshalResourceSlice(raw)
		if err != nil {
			return err
		}
		v.Contained = resources
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias RegulatedAuthorization
	return json.Unmarshal(residual, (*alias)(v))
}

// RegulatedAuthorizationCase is a generated nested backbone element.
type RegulatedAuthorizationCase struct {
	BackboneElement
	Identifier   *Identifier                  `json:"identifier,omitempty"`
	Type         *CodeableConcept             `json:"type,omitempty"`
	Status       *CodeableConcept             `json:"status,omitempty"`
	DatePeriod   *Period                      `json:"datePeriod,omitempty"`
	DateDateTime *FHIRDateTime                `json:"dateDateTime,omitempty"`
	Application  []RegulatedAuthorizationCase `json:"application,omitempty"`
}

// RegulatedAuthorizationCaseDate is the sealed value interface for the date[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isRegulatedAuthorizationCaseDate marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type RegulatedAuthorizationCaseDate interface{ isRegulatedAuthorizationCaseDate() }

func (Period) isRegulatedAuthorizationCaseDate()       {}
func (FHIRDateTime) isRegulatedAuthorizationCaseDate() {}

// Date returns the value set in the date[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *RegulatedAuthorizationCase) Date() (RegulatedAuthorizationCaseDate, bool) {
	switch {
	case r.DatePeriod != nil:
		return *r.DatePeriod, true
	case r.DateDateTime != nil:
		return *r.DateDateTime, true
	}
	return nil, false
}

// SetDatePeriod sets date[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *RegulatedAuthorizationCase) SetDatePeriod(v Period) {
	r.DatePeriod = nil
	r.DateDateTime = nil
	r.DatePeriod = &v
}

// SetDateDateTime sets date[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isRegulatedAuthorizationCaseDate marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *RegulatedAuthorizationCase) SetDateDateTime(v FHIRDateTime) {
	r.DatePeriod = nil
	r.DateDateTime = nil
	r.DateDateTime = &v
}
