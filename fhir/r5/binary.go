// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// BinaryResourceType is the FHIR resourceType discriminator for Binary.
const BinaryResourceType = "Binary"

// Binary is the generated FHIR Binary resource.
type Binary struct {
	Resource
	ContentType        *string                `json:"contentType,omitempty"`
	ContentTypeElement *fhir.PrimitiveElement `json:"-"`
	SecurityContext    *Reference             `json:"securityContext,omitempty"`
	Data               *string                `json:"data,omitempty"`
	DataElement        *fhir.PrimitiveElement `json:"-"`
}

// ResourceType returns the FHIR discriminator "Binary".
func (r *Binary) ResourceType() string { return BinaryResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a Binary never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *Binary) MarshalJSON() ([]byte, error) {
	type alias Binary
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: BinaryResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalBinarySiblings(r, encoded)
}

// marshalBinarySiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalBinarySiblings(v *Binary, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.ContentTypeElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_contentType", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.DataElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_data", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *Binary) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_contentType"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ContentTypeElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_data"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DataElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias Binary
	return json.Unmarshal(residual, (*alias)(v))
}
