// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// DataRequirement is the generated FHIR DataRequirement datatype.
type DataRequirement struct {
	Element
	Type                   *string                      `json:"type,omitempty"`
	TypeElement            *fhir.PrimitiveElement       `json:"-"`
	Profile                []string                     `json:"profile,omitempty"`
	ProfileElement         []*fhir.PrimitiveElement     `json:"-"`
	SubjectCodeableConcept *CodeableConcept             `json:"subjectCodeableConcept,omitempty"`
	SubjectReference       *Reference                   `json:"subjectReference,omitempty"`
	MustSupport            []string                     `json:"mustSupport,omitempty"`
	MustSupportElement     []*fhir.PrimitiveElement     `json:"-"`
	CodeFilter             []DataRequirementCodeFilter  `json:"codeFilter,omitempty"`
	DateFilter             []DataRequirementDateFilter  `json:"dateFilter,omitempty"`
	ValueFilter            []DataRequirementValueFilter `json:"valueFilter,omitempty"`
	Limit                  *int32                       `json:"limit,omitempty"`
	LimitElement           *fhir.PrimitiveElement       `json:"-"`
	Sort                   []DataRequirementSort        `json:"sort,omitempty"`
}

// MarshalJSON folds the primitive "_field" siblings into the encoded value: a scalar
// sibling is dropped when it carries no id or extension, and a repeating sibling's
// array is null-aligned with its value array, so a partially-extended repeating
// primitive lines up position-for-position on the wire (a position with no
// id/extension becomes a JSON null in the "_field" array).
func (v DataRequirement) MarshalJSON() ([]byte, error) {
	type alias DataRequirement
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalDataRequirementSiblings(&v, encoded)
}

// marshalDataRequirementSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDataRequirementSiblings(v *DataRequirement, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.TypeElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_type", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.LimitElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_limit", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtensions(len(v.Profile), v.ProfileElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_profile", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtensions(len(v.MustSupport), v.MustSupportElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_mustSupport", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *DataRequirement) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_type"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.TypeElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_limit"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.LimitElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_profile"); ok {
		elements, err := fhir.UnmarshalPrimitiveExtensions(raw)
		if err != nil {
			return err
		}
		v.ProfileElement = elements
	}
	if raw, ok := fhir.TakeRawField(obj, "_mustSupport"); ok {
		elements, err := fhir.UnmarshalPrimitiveExtensions(raw)
		if err != nil {
			return err
		}
		v.MustSupportElement = elements
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DataRequirement
	return json.Unmarshal(residual, (*alias)(v))
}

// DataRequirementSubject is the sealed value interface for the subject[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isDataRequirementSubject marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type DataRequirementSubject interface{ isDataRequirementSubject() }

func (CodeableConcept) isDataRequirementSubject() {}
func (Reference) isDataRequirementSubject()       {}

// Subject returns the value set in the subject[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *DataRequirement) Subject() (DataRequirementSubject, bool) {
	switch {
	case r.SubjectCodeableConcept != nil:
		return *r.SubjectCodeableConcept, true
	case r.SubjectReference != nil:
		return *r.SubjectReference, true
	}
	return nil, false
}

// SetSubjectCodeableConcept sets subject[x] to a CodeableConcept and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirement) SetSubjectCodeableConcept(v CodeableConcept) {
	r.SubjectCodeableConcept = nil
	r.SubjectReference = nil
	r.SubjectCodeableConcept = &v
}

// SetSubjectReference sets subject[x] to a Reference and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirement) SetSubjectReference(v Reference) {
	r.SubjectCodeableConcept = nil
	r.SubjectReference = nil
	r.SubjectReference = &v
}

// DataRequirementCodeFilter is a generated nested backbone element.
type DataRequirementCodeFilter struct {
	Element
	Path               *string                `json:"path,omitempty"`
	PathElement        *fhir.PrimitiveElement `json:"-"`
	SearchParam        *string                `json:"searchParam,omitempty"`
	SearchParamElement *fhir.PrimitiveElement `json:"-"`
	ValueSet           *string                `json:"valueSet,omitempty"`
	ValueSetElement    *fhir.PrimitiveElement `json:"-"`
	Code               []Coding               `json:"code,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v DataRequirementCodeFilter) MarshalJSON() ([]byte, error) {
	type alias DataRequirementCodeFilter
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalDataRequirementCodeFilterSiblings(&v, encoded)
}

// marshalDataRequirementCodeFilterSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDataRequirementCodeFilterSiblings(v *DataRequirementCodeFilter, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.PathElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_path", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.SearchParamElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_searchParam", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.ValueSetElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_valueSet", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *DataRequirementCodeFilter) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_path"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.PathElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_searchParam"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.SearchParamElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_valueSet"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ValueSetElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DataRequirementCodeFilter
	return json.Unmarshal(residual, (*alias)(v))
}

// DataRequirementDateFilter is a generated nested backbone element.
type DataRequirementDateFilter struct {
	Element
	Path               *string                `json:"path,omitempty"`
	PathElement        *fhir.PrimitiveElement `json:"-"`
	SearchParam        *string                `json:"searchParam,omitempty"`
	SearchParamElement *fhir.PrimitiveElement `json:"-"`
	ValueDateTime      *FHIRDateTime          `json:"valueDateTime,omitempty"`
	ValuePeriod        *Period                `json:"valuePeriod,omitempty"`
	ValueDuration      *Duration              `json:"valueDuration,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v DataRequirementDateFilter) MarshalJSON() ([]byte, error) {
	type alias DataRequirementDateFilter
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalDataRequirementDateFilterSiblings(&v, encoded)
}

// marshalDataRequirementDateFilterSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDataRequirementDateFilterSiblings(v *DataRequirementDateFilter, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.PathElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_path", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.SearchParamElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_searchParam", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *DataRequirementDateFilter) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_path"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.PathElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_searchParam"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.SearchParamElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DataRequirementDateFilter
	return json.Unmarshal(residual, (*alias)(v))
}

// DataRequirementDateFilterValue is the sealed value interface for the value[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isDataRequirementDateFilterValue marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type DataRequirementDateFilterValue interface{ isDataRequirementDateFilterValue() }

func (FHIRDateTime) isDataRequirementDateFilterValue() {}
func (Period) isDataRequirementDateFilterValue()       {}
func (Duration) isDataRequirementDateFilterValue()     {}

// Value returns the value set in the value[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *DataRequirementDateFilter) Value() (DataRequirementDateFilterValue, bool) {
	switch {
	case r.ValueDateTime != nil:
		return *r.ValueDateTime, true
	case r.ValuePeriod != nil:
		return *r.ValuePeriod, true
	case r.ValueDuration != nil:
		return *r.ValueDuration, true
	}
	return nil, false
}

// SetValueDateTime sets value[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isDataRequirementDateFilterValue marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirementDateFilter) SetValueDateTime(v FHIRDateTime) {
	r.ValueDateTime = nil
	r.ValuePeriod = nil
	r.ValueDuration = nil
	r.ValueDateTime = &v
}

// SetValuePeriod sets value[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirementDateFilter) SetValuePeriod(v Period) {
	r.ValueDateTime = nil
	r.ValuePeriod = nil
	r.ValueDuration = nil
	r.ValuePeriod = &v
}

// SetValueDuration sets value[x] to a Duration and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirementDateFilter) SetValueDuration(v Duration) {
	r.ValueDateTime = nil
	r.ValuePeriod = nil
	r.ValueDuration = nil
	r.ValueDuration = &v
}

// DataRequirementSort is a generated nested backbone element.
type DataRequirementSort struct {
	Element
	Path             *string                `json:"path,omitempty"`
	PathElement      *fhir.PrimitiveElement `json:"-"`
	Direction        *string                `json:"direction,omitempty"`
	DirectionElement *fhir.PrimitiveElement `json:"-"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v DataRequirementSort) MarshalJSON() ([]byte, error) {
	type alias DataRequirementSort
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalDataRequirementSortSiblings(&v, encoded)
}

// marshalDataRequirementSortSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDataRequirementSortSiblings(v *DataRequirementSort, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.PathElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_path", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.DirectionElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_direction", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *DataRequirementSort) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_path"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.PathElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_direction"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.DirectionElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DataRequirementSort
	return json.Unmarshal(residual, (*alias)(v))
}

// DataRequirementValueFilter is a generated nested backbone element.
type DataRequirementValueFilter struct {
	Element
	Path               *string                `json:"path,omitempty"`
	PathElement        *fhir.PrimitiveElement `json:"-"`
	SearchParam        *string                `json:"searchParam,omitempty"`
	SearchParamElement *fhir.PrimitiveElement `json:"-"`
	Comparator         *string                `json:"comparator,omitempty"`
	ComparatorElement  *fhir.PrimitiveElement `json:"-"`
	ValueDateTime      *FHIRDateTime          `json:"valueDateTime,omitempty"`
	ValuePeriod        *Period                `json:"valuePeriod,omitempty"`
	ValueDuration      *Duration              `json:"valueDuration,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v DataRequirementValueFilter) MarshalJSON() ([]byte, error) {
	type alias DataRequirementValueFilter
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalDataRequirementValueFilterSiblings(&v, encoded)
}

// marshalDataRequirementValueFilterSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalDataRequirementValueFilterSiblings(v *DataRequirementValueFilter, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.PathElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_path", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.SearchParamElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_searchParam", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.ComparatorElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_comparator", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *DataRequirementValueFilter) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_path"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.PathElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_searchParam"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.SearchParamElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_comparator"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ComparatorElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias DataRequirementValueFilter
	return json.Unmarshal(residual, (*alias)(v))
}

// DataRequirementValueFilterValue is the sealed value interface for the value[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isDataRequirementValueFilterValue marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type DataRequirementValueFilterValue interface{ isDataRequirementValueFilterValue() }

func (FHIRDateTime) isDataRequirementValueFilterValue() {}
func (Period) isDataRequirementValueFilterValue()       {}
func (Duration) isDataRequirementValueFilterValue()     {}

// Value returns the value set in the value[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *DataRequirementValueFilter) Value() (DataRequirementValueFilterValue, bool) {
	switch {
	case r.ValueDateTime != nil:
		return *r.ValueDateTime, true
	case r.ValuePeriod != nil:
		return *r.ValuePeriod, true
	case r.ValueDuration != nil:
		return *r.ValueDuration, true
	}
	return nil, false
}

// SetValueDateTime sets value[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isDataRequirementValueFilterValue marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirementValueFilter) SetValueDateTime(v FHIRDateTime) {
	r.ValueDateTime = nil
	r.ValuePeriod = nil
	r.ValueDuration = nil
	r.ValueDateTime = &v
}

// SetValuePeriod sets value[x] to a Period and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirementValueFilter) SetValuePeriod(v Period) {
	r.ValueDateTime = nil
	r.ValuePeriod = nil
	r.ValueDuration = nil
	r.ValuePeriod = &v
}

// SetValueDuration sets value[x] to a Duration and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *DataRequirementValueFilter) SetValueDuration(v Duration) {
	r.ValueDateTime = nil
	r.ValuePeriod = nil
	r.ValueDuration = nil
	r.ValueDuration = &v
}
