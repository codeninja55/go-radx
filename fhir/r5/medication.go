// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// MedicationResourceType is the FHIR resourceType discriminator for Medication.
const MedicationResourceType = "Medication"

// Medication is the generated FHIR Medication resource.
type Medication struct {
	DomainResource
	Identifier                   []Identifier           `json:"identifier,omitempty"`
	Code                         *CodeableConcept       `json:"code,omitempty"`
	Status                       *string                `json:"status,omitempty"`
	StatusElement                *fhir.PrimitiveElement `json:"-"`
	MarketingAuthorizationHolder *Reference             `json:"marketingAuthorizationHolder,omitempty"`
	DoseForm                     *CodeableConcept       `json:"doseForm,omitempty"`
	TotalVolume                  *Quantity              `json:"totalVolume,omitempty"`
	Ingredient                   []MedicationIngredient `json:"ingredient,omitempty"`
	Batch                        *MedicationBatch       `json:"batch,omitempty"`
	Definition                   *Reference             `json:"definition,omitempty"`
}

// ResourceType returns the FHIR discriminator "Medication".
func (r *Medication) ResourceType() string { return MedicationResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a Medication never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *Medication) MarshalJSON() ([]byte, error) {
	type alias Medication
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: MedicationResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalMedicationSiblings(r, encoded)
}

// marshalMedicationSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalMedicationSiblings(v *Medication, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.StatusElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_status", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *Medication) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_status"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.StatusElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias Medication
	return json.Unmarshal(residual, (*alias)(v))
}

// MedicationBatch is a generated nested backbone element.
type MedicationBatch struct {
	BackboneElement
	LotNumber             *string                `json:"lotNumber,omitempty"`
	LotNumberElement      *fhir.PrimitiveElement `json:"-"`
	ExpirationDate        *string                `json:"expirationDate,omitempty"`
	ExpirationDateElement *fhir.PrimitiveElement `json:"-"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v MedicationBatch) MarshalJSON() ([]byte, error) {
	type alias MedicationBatch
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalMedicationBatchSiblings(&v, encoded)
}

// marshalMedicationBatchSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalMedicationBatchSiblings(v *MedicationBatch, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.LotNumberElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_lotNumber", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.ExpirationDateElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_expirationDate", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *MedicationBatch) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_lotNumber"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.LotNumberElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_expirationDate"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ExpirationDateElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias MedicationBatch
	return json.Unmarshal(residual, (*alias)(v))
}

// MedicationIngredient is a generated nested backbone element.
type MedicationIngredient struct {
	BackboneElement
	Item            *CodeableReference     `json:"item,omitempty"`
	IsActive        *bool                  `json:"isActive,omitempty"`
	IsActiveElement *fhir.PrimitiveElement `json:"-"`
	Strength        *Ratio                 `json:"strength,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v MedicationIngredient) MarshalJSON() ([]byte, error) {
	type alias MedicationIngredient
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalMedicationIngredientSiblings(&v, encoded)
}

// marshalMedicationIngredientSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalMedicationIngredientSiblings(v *MedicationIngredient, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.IsActiveElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_isActive", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *MedicationIngredient) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_isActive"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.IsActiveElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias MedicationIngredient
	return json.Unmarshal(residual, (*alias)(v))
}
