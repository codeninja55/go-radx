// Code generated by fhir-gen; DO NOT EDIT.

package r5

import (
	"encoding/json"
	"github.com/codeninja55/go-radx/fhir"
)

// PatientResourceType is the FHIR resourceType discriminator for Patient.
const PatientResourceType = "Patient"

// Patient is the generated FHIR Patient resource.
type Patient struct {
	DomainResource
	Identifier           []Identifier           `json:"identifier,omitempty"`
	Active               *bool                  `json:"active,omitempty"`
	ActiveElement        *fhir.PrimitiveElement `json:"-"`
	Name                 []HumanName            `json:"name,omitempty"`
	Telecom              []ContactPoint         `json:"telecom,omitempty"`
	Gender               *string                `json:"gender,omitempty"`
	GenderElement        *fhir.PrimitiveElement `json:"-"`
	BirthDate            *string                `json:"birthDate,omitempty"`
	BirthDateElement     *fhir.PrimitiveElement `json:"-"`
	DeceasedBoolean      *FHIRBoolean           `json:"deceasedBoolean,omitempty"`
	DeceasedDateTime     *FHIRDateTime          `json:"deceasedDateTime,omitempty"`
	Address              []Address              `json:"address,omitempty"`
	MaritalStatus        *CodeableConcept       `json:"maritalStatus,omitempty"`
	MultipleBirthBoolean *FHIRBoolean           `json:"multipleBirthBoolean,omitempty"`
	MultipleBirthInteger *FHIRInteger           `json:"multipleBirthInteger,omitempty"`
	Photo                []Attachment           `json:"photo,omitempty"`
	Contact              []PatientContact       `json:"contact,omitempty"`
	Communication        []PatientCommunication `json:"communication,omitempty"`
	GeneralPractitioner  []Reference            `json:"generalPractitioner,omitempty"`
	ManagingOrganization *Reference             `json:"managingOrganization,omitempty"`
	Link                 []PatientLink          `json:"link,omitempty"`
}

// ResourceType returns the FHIR discriminator "Patient".
func (r *Patient) ResourceType() string { return PatientResourceType }

// MarshalJSON emits the resource with "resourceType" as the first JSON key, always
// carrying the type constant — even for a zero value, so a Patient never
// serialises an empty "resourceType". The primitive "_field" siblings are folded in
// afterwards: a scalar sibling is dropped when it carries no id or extension, and a
// repeating sibling's array is null-aligned with its value array so positions line
// up on the wire.
func (r *Patient) MarshalJSON() ([]byte, error) {
	type alias Patient
	encoded, err := json.Marshal(struct {
		ResourceType string `json:"resourceType"`
		*alias
	}{
		ResourceType: PatientResourceType,
		alias:        (*alias)(r),
	})
	if err != nil {
		return nil, err
	}
	return marshalPatientSiblings(r, encoded)
}

// marshalPatientSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalPatientSiblings(v *Patient, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.ActiveElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_active", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.GenderElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_gender", Value: raw})
	}
	if raw, err := fhir.MarshalPrimitiveExtension(v.BirthDateElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_birthDate", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *Patient) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_active"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.ActiveElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_gender"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.GenderElement = &element
	}
	if raw, ok := fhir.TakeRawField(obj, "_birthDate"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.BirthDateElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias Patient
	return json.Unmarshal(residual, (*alias)(v))
}

// PatientDeceased is the sealed value interface for the deceased[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isPatientDeceased marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type PatientDeceased interface{ isPatientDeceased() }

func (FHIRBoolean) isPatientDeceased()  {}
func (FHIRDateTime) isPatientDeceased() {}

// Deceased returns the value set in the deceased[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *Patient) Deceased() (PatientDeceased, bool) {
	switch {
	case r.DeceasedBoolean != nil:
		return *r.DeceasedBoolean, true
	case r.DeceasedDateTime != nil:
		return *r.DeceasedDateTime, true
	}
	return nil, false
}

// SetDeceasedBoolean sets deceased[x] to a FHIRBoolean (the
// release primitive wrapper that carries the isPatientDeceased marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *Patient) SetDeceasedBoolean(v FHIRBoolean) {
	r.DeceasedBoolean = nil
	r.DeceasedDateTime = nil
	r.DeceasedBoolean = &v
}

// SetDeceasedDateTime sets deceased[x] to a FHIRDateTime (the
// release primitive wrapper that carries the isPatientDeceased marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *Patient) SetDeceasedDateTime(v FHIRDateTime) {
	r.DeceasedBoolean = nil
	r.DeceasedDateTime = nil
	r.DeceasedDateTime = &v
}

// PatientMultipleBirth is the sealed value interface for the multipleBirth[x]
// choice group. It is implemented only by this package's branch types — the named
// datatype structs and the release primitive wrappers — through the unexported
// isPatientMultipleBirth marker, so a built-in scalar can never satisfy it and the
// branch set stays closed.
type PatientMultipleBirth interface{ isPatientMultipleBirth() }

func (FHIRBoolean) isPatientMultipleBirth() {}
func (FHIRInteger) isPatientMultipleBirth() {}

// MultipleBirth returns the value set in the multipleBirth[x] choice
// group, or (nil, false) when no branch is set. The returned value is one of the
// branch types; a type switch recovers which branch was chosen.
func (r *Patient) MultipleBirth() (PatientMultipleBirth, bool) {
	switch {
	case r.MultipleBirthBoolean != nil:
		return *r.MultipleBirthBoolean, true
	case r.MultipleBirthInteger != nil:
		return *r.MultipleBirthInteger, true
	}
	return nil, false
}

// SetMultipleBirthBoolean sets multipleBirth[x] to a FHIRBoolean (the
// release primitive wrapper that carries the isPatientMultipleBirth marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *Patient) SetMultipleBirthBoolean(v FHIRBoolean) {
	r.MultipleBirthBoolean = nil
	r.MultipleBirthInteger = nil
	r.MultipleBirthBoolean = &v
}

// SetMultipleBirthInteger sets multipleBirth[x] to a FHIRInteger (the
// release primitive wrapper that carries the isPatientMultipleBirth marker; the built-in
// scalar cannot) and clears every other branch, so the group holds at most one
// value and marshals exactly one suffixed key.
func (r *Patient) SetMultipleBirthInteger(v FHIRInteger) {
	r.MultipleBirthBoolean = nil
	r.MultipleBirthInteger = nil
	r.MultipleBirthInteger = &v
}

// PatientCommunication is a generated nested backbone element.
type PatientCommunication struct {
	BackboneElement
	Language         *CodeableConcept       `json:"language,omitempty"`
	Preferred        *bool                  `json:"preferred,omitempty"`
	PreferredElement *fhir.PrimitiveElement `json:"-"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v PatientCommunication) MarshalJSON() ([]byte, error) {
	type alias PatientCommunication
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalPatientCommunicationSiblings(&v, encoded)
}

// marshalPatientCommunicationSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalPatientCommunicationSiblings(v *PatientCommunication, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.PreferredElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_preferred", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *PatientCommunication) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_preferred"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.PreferredElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias PatientCommunication
	return json.Unmarshal(residual, (*alias)(v))
}

// PatientContact is a generated nested backbone element.
type PatientContact struct {
	BackboneElement
	Relationship  []CodeableConcept      `json:"relationship,omitempty"`
	Name          *HumanName             `json:"name,omitempty"`
	Telecom       []ContactPoint         `json:"telecom,omitempty"`
	Address       *Address               `json:"address,omitempty"`
	Gender        *string                `json:"gender,omitempty"`
	GenderElement *fhir.PrimitiveElement `json:"-"`
	Organization  *Reference             `json:"organization,omitempty"`
	Period        *Period                `json:"period,omitempty"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v PatientContact) MarshalJSON() ([]byte, error) {
	type alias PatientContact
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalPatientContactSiblings(&v, encoded)
}

// marshalPatientContactSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalPatientContactSiblings(v *PatientContact, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.GenderElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_gender", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *PatientContact) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_gender"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.GenderElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias PatientContact
	return json.Unmarshal(residual, (*alias)(v))
}

// PatientLink is a generated nested backbone element.
type PatientLink struct {
	BackboneElement
	Other       *Reference             `json:"other,omitempty"`
	Type        *string                `json:"type,omitempty"`
	TypeElement *fhir.PrimitiveElement `json:"-"`
}

// MarshalJSON folds the backbone's primitive "_field" siblings into the encoded
// value: scalar siblings dropped when empty, repeating siblings null-aligned with
// their value arrays.
func (v PatientLink) MarshalJSON() ([]byte, error) {
	type alias PatientLink
	encoded, err := json.Marshal(alias(v))
	if err != nil {
		return nil, err
	}
	return marshalPatientLinkSiblings(&v, encoded)
}

// marshalPatientLinkSiblings appends the primitive "_field" siblings to the
// already-encoded object, preserving the value fields' canonical order: an empty
// scalar sibling and an un-extended repeating sibling are skipped, and a repeating
// sibling's array is null-aligned with its value array.
func marshalPatientLinkSiblings(v *PatientLink, encoded []byte) ([]byte, error) {
	var siblings []fhir.RawSibling
	if raw, err := fhir.MarshalPrimitiveExtension(v.TypeElement); err != nil {
		return nil, err
	} else {
		siblings = append(siblings, fhir.RawSibling{Key: "_type", Value: raw})
	}
	return fhir.AppendSiblings(encoded, siblings)
}

// UnmarshalJSON lifts each primitive "_field" sibling out of the object, decodes it
// into its companion field, then decodes the remaining keys into the value struct.
func (v *PatientLink) UnmarshalJSON(data []byte) error {
	obj, err := fhir.SplitRawObject(data)
	if err != nil {
		return err
	}
	if raw, ok := fhir.TakeRawField(obj, "_type"); ok {
		var element fhir.PrimitiveElement
		if err := json.Unmarshal(raw, &element); err != nil {
			return err
		}
		v.TypeElement = &element
	}
	residual, err := fhir.RemarshalObject(obj)
	if err != nil {
		return err
	}
	type alias PatientLink
	return json.Unmarshal(residual, (*alias)(v))
}
